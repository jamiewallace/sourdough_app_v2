= form_for([@post, @post.comments.build]) do |f|
  .field
    = f.label :commenter
    %br
    = f.text_field :commenter
  .field
    = f.label :text
    %br
    = f.text_area :text
  .actions
    = f.submit 'Post comment'
 